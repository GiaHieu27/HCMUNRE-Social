import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    successCustom: {
      main: '#29c031',
    },
    greyCustom: {
      main: '#bcc0c4',
    },
  },
});

export default theme;
