import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    successCustom: {
      main: '#29c031',
      contrastText: '#fff',
    },
    greyCustom: {
      main: '#a6a9ad',
    },
  },
});

export default theme;
